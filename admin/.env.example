# Auth secret — use the same value as the main app
AUTH_SECRET=

# Shared database (path is relative to prisma/schema.prisma)
DATABASE_URL="file:./dev.db"

# Temporary admin login (change before production)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123456
